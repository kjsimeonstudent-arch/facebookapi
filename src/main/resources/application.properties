spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://dpg-d464kk8dl3ps738pp4p0-a.oregon-postgres.render.com:5432/db_post?sslmode=require}
spring.datasource.username=${DB_USERNAME:Ken-zhin}
spring.datasource.password=${DB_PASSWORD:admin}         
server.port=8000
spring.application.name=facebookapi
#update the value above then from below, paste provided code

spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Optional: Connection pool settings (HikariCP)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.connection-test-query=SELECT 1
